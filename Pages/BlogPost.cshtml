@page
@model Bloggen.Pages.BlogPostModel
@{
}
<div class="container">
    <div class="row">
        <div class="border">
            <img src="~/img/@Model.Blog.Image" width="800" />
            <h3>@Model.Blog.Header</h3>
            <p>@Model.Blog.Text</p>
            <small>Publicerad: @Model.Blog.Date</small>
        </div>
    </div>
</div>